{
  "name": "video-service",
  "version": "1.0.0",
  "description": "A microservice for streaming video",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env ./src/index.js",
    "dev": "nodemon --env-file=.env ./src/index.js"
  },
  "author": "Lucas Climenhage",
  "license": "ISC",
  "dependencies": {
    "express": "^5.0.0-beta.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
